# Changelog

## 0.1.0

- Reorganized scripts into a GitHub-ready repo structure.
- Added `lib/` folder expected by the main launcher.
- Added venv-style `activate.ps1`.
- Added install/uninstall scripts for a persistent `flookOFF` terminal command.
- Added `flookOFF.config.json` defaults.
- Added `.gitignore`, `.gitattributes`, `.editorconfig`, README, LICENSE, and GitHub Actions lint workflow.
- Added JSON as a valid `-Format` value.
- Removed process-level execution-policy bypass from the main launcher.
